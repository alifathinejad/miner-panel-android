"""
نقطه‌ی ورود اپ اندروید.

این فایل هیچ منطق جدیدی ندارد و فقط همان فایل اصلی پنل (miner_panel_core.py --
دقیقاً همان کدی که روی اوبونتو/ویندوز اجرا می‌شود، بدون کوچک‌ترین تغییری) را
اجرا می‌کند.

نکته: از runpy.run_module (نه run_path) استفاده می‌کنیم چون Buildozer بعد از
کامپایل، فایل اصلی .py را حذف و فقط نسخه‌ی .pyc را نگه می‌دارد (به‌جز خود
main.py). run_module از سیستم import خود پایتون استفاده می‌کند که هم .py و
هم .pyc را پیدا می‌کند، برخلاف run_path که دنبال اسم دقیق فایل می‌گردد.
"""
import runpy

runpy.run_module("miner_panel_core", run_name="__main__")
