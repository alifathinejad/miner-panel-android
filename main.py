"""
نقطه‌ی ورود اپ اندروید.

این فایل هیچ منطق جدیدی ندارد و فقط همان فایل اصلی پنل (miner_panel_core.py --
دقیقاً همان کدی که روی اوبونتو/ویندوز اجرا می‌شود، بدون کوچک‌ترین تغییری) را
اجرا می‌کند. چون هیچ گواهی SSL داخل اپ نیست، برنامه به‌صورت خودکار طبق منطق
موجودش روی HTTP ساده و پورت 2096 بالا می‌آید -- همان پورتی که در buildozer.spec
(p4a.port) به بوت‌استرپ webview گفته شده تا صفحه را داخل اپ نمایش دهد.

اگر بعداً بخواهید پورت پیش‌فرض را عوض کنید، هم این عدد و هم مقدار p4a.port در
buildozer.spec باید همزمان عوض شوند.
"""
import runpy

runpy.run_path("miner_panel_core.py", run_name="__main__")
